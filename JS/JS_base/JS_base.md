# JS_base

## Вопрос 1
Какие значения может содержать переменная типа string в JavaScript?

- Только латинские буквы и цифры
- Только последовательности букв
- Только значения, заключенные в двойные кавычки.
- Последовательности символов, заключенные в кавычки
- Только одиночные символы

---

## Вопрос 2
В каком случае переменная перестанет существовать после завершения работы блока?

- Если она объявлена внутри функции с var
- Если она объявлена без ключевого слова
- Если она объявлена в глобальной области видимости
- Переменная, объявленная с let и const
- Переменная, объявленная с var

---

## Вопрос 3
Вы разрабатываете систему онлайн-тестирования. Требуется временно хранить ответы пользователя, чтобы при случайном обновлении страницы он мог продолжить с того же места. Данные должны удаляться после завершения теста. Какое хранилище данных лучше всего подойдет?

- JSON-файл
- SessionStorage
- IndexedDB
- Server-side session storage
- Cookies

---

## Вопрос 4
Какое значение примет переменная result после выполнения следующего кода?

1 const x = 20;
2 const y = 6;
3 const result = x % y + Math.floor(x / y) * 2;

- 8
- 12
- 10
- 4
- 6

---

## Вопрос 5
Вам нужно инвертировать логическое значение переменной `isActive`. Какой оператор вы используете?

- isActive !=
- isActive = !isActive
- isActive = ~isActive
- isActive = --isActive
- isActive.not()

---

---

## Вопрос 6

Какой оператор нужно вставить, чтобы выражение вернуло true?
```
const value \= 'abc' \_\_\_ 'ABC';
```
- \==  
- \>  
- \===  
- \<=  
- \<

---

## Вопрос 7
Что будет выведено в консоль при выполнении следующего кода?

1 const b = 5;
2 if (b > 0 || b < -10) {
3   console.log("b положительное или меньше -10");
4 } else {
5   console.log("b не соответствует условиям");
6 }

- Будет выброшена ошибка синтаксиса
- Код зациклится и не завершится
- В консоль будет выведено «b не соответствует условиям»
- В консоль будет выведено «b положительное или меньше -10»
- Код не выполнится из-за неверного использования if

---

## Вопрос 8
Вы создаете программу, которая должна:

* Проверять, находится ли число в определенном диапазоне.
* Если число не соответствует условиям, предложить ввести новое значение.
* Завершить выполнение, когда введенное число соответствует заданному критерию.

Какой оператор управления потоком лучше использовать?

- switch-case
- try-catch
- while
- for
- do-while

---

## Вопрос 9
Какой вариант объявления функции с параметром по умолчанию корректный?

- let greet(name == "Гость") { return "Привет, " + name; }
- const greet = (name: "Гость") => "Привет, " + name;
- function greet(name = "Гость") { return "Привет, " + name; }
- function greet(name) = "Гость" { return "Привет, " + name; }
- function greet(name) { name = name ?? "Гость"; return "Привет, " + name; }

---

## Вопрос 10
Какое значение будет выведено в консоль при выполнении кода?

1 const numbers = [1, 2, 3];
2 const doubledNumbers = numbers.map(function(num) {
3   return num * 2;
4 });
5 console.log(doubledNumbers[0]);

- 1
- 2
- 3
- undefined
- Ошибка

---

## Вопрос 11
Вы разрабатываете веб-приложение для бронирования билетов в кинотеатр. Пользователь выбирает место и заполняет данные, но перед оплатой может случайно закрыть вкладку или перезагрузить страницу. Какое хранилище данных лучше использовать, чтобы информация о бронировании сохранялась только во время текущей работы с сайтом?

- IndexedDB
- SessionStorage
- Куки (cookies)
- In-memory object
- JSON-файл

---

## Вопрос 12
Какое значение примет переменная result после выполнения следующего кода?

1 const a = 15;
2 const b = 4;
3 const result = Math.floor(a / b) + (a % b) * 3;

- 5
- 6
- 9
- 8
- 12

---

## Вопрос 13
Вам нужно побитово сравнить два числа и вернуть 1 в позициях, где только один из битов равен 1. Какой оператор используете?

- &
- ^
- |
- ||
- &&

---

## Вопрос 14
Какой оператор нужно подставить, чтобы выражение вернуло true?

1 const result = 0 ___ "0";

- ==
- >
- ===
- <
- >=

---

## Вопрос 15
Что происходит при выполнении следующего кода?

1 const a = 35;
2 if (a > 10 && a < 30) {
3   console.log("a находится в диапазоне от 10 до 30");
4 } else {
5   console.log("a не находится в диапазоне от 10 до 30");
6 }

- Будет выброшена ошибка синтаксиса
- Зависит от значения переменной a
- В консоль будет выведено "a находится в диапазоне от 10 до 30"
- В консоль будет выведено "a не находится в диапазоне от 10 до 30"
- Код не выполнится, так как условие всегда false

---

## Вопрос 16
Какое значение будет выведено в консоль при выполнении кода?

1 function createCounter() {
2   let count = 0;
3   return function() {
4     count++;
5     console.log(count);
6   };
7 }
8 const counter = createCounter();
9 counter();
10 counter();

- Ошибка: count не определен
- 2 3
- 1 2
- undefined undefined
- 0 1